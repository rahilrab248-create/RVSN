Place club logo images in this folder.

The marquee reads `logoUrl` from `src/config/home.ts`.
You can replace these SVG badge placeholders with your own PNG, SVG, or WebP files and keep the same filenames.
