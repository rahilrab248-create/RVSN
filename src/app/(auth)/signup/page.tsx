import { SignupForm } from "@/components/auth/signup-form";

export const metadata = {
  title: "Create Account",
  description: "Create your Fooltball account.",
};

export default function SignupPage() {
  return <SignupForm />;
}
